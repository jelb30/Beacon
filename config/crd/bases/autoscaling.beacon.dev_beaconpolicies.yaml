---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: beaconpolicies.autoscaling.beacon.dev
spec:
  group: autoscaling.beacon.dev
  names:
    kind: BeaconPolicy
    listKind: BeaconPolicyList
    plural: beaconpolicies
    singular: beaconpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .status.lastDecision
      name: LastDecision
      type: string
    - jsonPath: .status.lastReactionLatencyMillis
      name: LatencyMs
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BeaconPolicy defines how Beacon should react to starvation signals.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec contains the requested scaling policy.
            properties:
              maxCPURequest:
                type: string
              maxMemoryRequest:
                type: string
              minCPURequest:
                type: string
              minMemoryRequest:
                type: string
              reactionLatencyBudgetMillis:
                format: int32
                minimum: 1
                type: integer
              scaleUpStepPercent:
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              starvationWindowSeconds:
                format: int32
                minimum: 1
                type: integer
              targetRef:
                description: BeaconPolicyTargetRef points to the workload Beacon can
                  scale.
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - maxCPURequest
            - minCPURequest
            - reactionLatencyBudgetMillis
            - scaleUpStepPercent
            - starvationWindowSeconds
            - targetRef
            type: object
          status:
            description: status contains Beacon's latest observed result.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastDecision:
                type: string
              lastEventName:
                type: string
              lastEventObservedAt:
                format: date-time
                type: string
              lastEventSeverity:
                type: string
              lastPatchedContainer:
                type: string
              lastPatchedDeployment:
                type: string
              lastReactionLatencyMillis:
                format: int64
                type: integer
              lastReconcileTime:
                format: date-time
                type: string
              lastScaledAt:
                format: date-time
                type: string
              lastSignalType:
                type: string
              newCPURequest:
                type: string
              newMemoryRequest:
                type: string
              observedGeneration:
                format: int64
                type: integer
              previousCPURequest:
                type: string
              previousMemoryRequest:
                type: string
              scaleAction:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
